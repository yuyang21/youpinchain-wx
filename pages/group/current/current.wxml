<view>
  <swiper indicator-dots="true" autoplay="true" interval="1500" duration="10">
    <block wx:for="{{brand}}" wx:key="{{id}}">
      <swiper-item>
        <image src="{{item.pictureUrl}}" class="slide-image" width="100%" />
      </swiper-item>
    </block>
  </swiper>
</view>
